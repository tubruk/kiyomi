{
  "name": "kiyomi",
  "version": "1.0.0",
  "private": true,
  "description": "Lightweight API-first manga reader and source aggregator platform powered by Go and React",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev:plugins": "KIYOMI_PLUGIN_DIR=dev-plugins CGO_ENABLED=0 go run ./scripts/watch-plugins.go",
    "dev:api": "KIYOMI_PLUGIN_DIR=dev-plugins CGO_ENABLED=0 go run ./cmd/kiyomi",
    "dev:ui": "bun --cwd web dev",
    "dev": "concurrently -n \"PLUGINS,API,UI\" -c \"yellow.bold,magenta.bold,cyan.bold\" \"bun dev:plugins\" \"bun dev:api\" \"bun dev:ui\"",
    "build:ui": "bun --cwd web build",
    "build:api": "CGO_ENABLED=0 go build -o bin/kiyomi ./cmd/kiyomi",
    "build": "bun run build:ui && bun run build:api",
    "test": "CGO_ENABLED=0 go test -v ./...",
    "e2e": "bash e2e/run.sh"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
